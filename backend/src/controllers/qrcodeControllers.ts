import {Request, Response} from "express"
import User from "../models/User"
import Jwt from "jsonwebtoken"
import bcrypt from "bcrypt"

export const generate = (req:Request, res:Response)=>{
    res.json("")
}
export const allQrcodes = (req:Request, res:Response)=>{
    res.json("")
}
export const specificQrcode = (req:Request, res:Response)=>{
    res.json("")
}

export const updateQrcode = (req:Request, res:Response)=>{
    res.json("")
}
export const deleteQrcode = (req:Request, res:Response)=>{
    res.json("")
}

